import React from "react";
import type { FormFieldProps } from "./form-types";
export declare const FormField: ({ name, label, required, className, children, rules, }: FormFieldProps) => React.JSX.Element;
